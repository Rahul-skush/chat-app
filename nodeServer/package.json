{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "this is an node server for chat app",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "author": "rahul",
  "license": "ISC",
  "dependencies": {
    "react-scripts": "^5.0.1",
    "socket.io": "^2.4.0"
  }
}
